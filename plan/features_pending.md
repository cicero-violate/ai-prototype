1. Planning
   goal → task graph → executable steps → receipts → replanning

2. Retrieval
   tlog/query → semantic memory → relevant context → LLM prompt

3. Learning gradients
   outcome delta → score delta → policy update signal → promoted rule

4. Policy evolution
   verified repeated success → candidate policy → gated promotion

5. Multi-step LLM execution
   plan step → LLM/tool call → receipt → verify → next step

6. Human-readable tlog decoder
   numeric tlog → readable event/effect JSON

7. Memory compression
   raw tlog → summaries → embeddings/index → durable memory

8. Replay-based regression tests
   tamper/edit/drift → deterministic failure

9. Evaluation loop
   run → score → weakest dimension → next patch target

10. Scheduler/orchestrator
   pending tasks → priority → execution budget → closure
